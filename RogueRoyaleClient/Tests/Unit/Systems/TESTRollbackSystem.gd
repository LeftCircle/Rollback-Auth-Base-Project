extends GutTestRogue


func test_server_components_are_added_to_characters_before_rollback_reset() -> void:
	# Assert that a component from the server is added to the entity
	# before components are reset to the rollback frame. 
	assert_true(false, "Not yet implemented")
